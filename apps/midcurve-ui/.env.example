# =============================================================================
# Midcurve UI Environment Variables (Vite SPA)
# =============================================================================

# =============================================================================
# API Configuration
# =============================================================================
# URL of the Midcurve API server
# In development with Vite proxy: leave empty (uses /api proxy)
# In production: set to API server URL (e.g., https://api.midcurve.finance)
VITE_API_URL=

# =============================================================================
# WalletConnect (RainbowKit) - Required
# =============================================================================
# Get your project ID from: https://cloud.walletconnect.com
VITE_WALLETCONNECT_PROJECT_ID=your-walletconnect-project-id

# =============================================================================
# Local Development Chain (OPTIONAL)
# =============================================================================
# Enable local Anvil chain for development testing
# Set to "true" to add Local Testnet (chain 31337) to the wallet options
#
# Start Anvil fork with: cd apps/midcurve-automation && pnpm local:anvil
# Run setup script: cd apps/midcurve-automation && pnpm local:setup
# VITE_ENABLE_LOCAL_CHAIN=true
# VITE_LOCAL_RPC_URL=http://localhost:8545

# Local chain mock contracts (auto-generated by local:setup script)
# Required for hedge vault deployment on local chain
# VITE_MOCK_AUGUSTUS_REGISTRY_ADDRESS=0x...
