/**
 * Format helpers - proxy to @midcurve/shared
 */
export {
  formatCurrency,
  formatPnL,
  formatFees,
  formatPercentage,
  calculateAPR,
  formatTokenAmount,
  formatFeeTier,
  formatUSDValue,
  formatProtocolName,
} from '@midcurve/shared';
