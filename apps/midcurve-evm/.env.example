# SEMSEE Embedded EVM Configuration
#
# Copy this file to .env and customize as needed.
# Default values use Foundry's first default account (for local development).

# =============================================================================
# Core Identity (Orchestrator)
# =============================================================================

# Core Private Key
# The private key used by the orchestrator to sign transactions.
# Must correspond to the CORE address hardcoded in CoreControlled.sol.
# Default: Foundry's default account 0 (pre-funded in Anvil)
CORE_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Core Address (derived from CORE_PRIVATE_KEY)
# Must match the address in CoreControlled.sol.
# Default: Foundry's default account 0
CORE_ADDRESS=0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266

# =============================================================================
# Using a Custom Key
# =============================================================================
# To use a custom private key:
# 1. Generate a new private key
# 2. Derive the address from the private key
# 3. Update CORE_PRIVATE_KEY and CORE_ADDRESS above
# 4. Update CoreControlled.sol with the new CORE address (must recompile contracts)
# 5. Rebuild Docker images: docker-compose build --no-cache
# 6. Deploy contracts: npm run up

# =============================================================================
# RabbitMQ Configuration
# =============================================================================

# Connection settings
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=midcurve
RABBITMQ_PASS=midcurve_dev
RABBITMQ_VHOST=/

# Management UI (optional, for debugging)
RABBITMQ_MANAGEMENT_PORT=15672

# Connection URL (constructed from above, for convenience)
# RABBITMQ_URL=amqp://midcurve:midcurve_dev@localhost:5672/

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL connection string for Prisma
# Required for strategy lookups and state management
DATABASE_URL="postgresql://devuser:devpass@localhost:5432/midcurve_dev?schema=public"

# =============================================================================
# Signer Service Configuration
# =============================================================================

# URL for the signer service (required for strategy deployment)
# The signer service handles transaction signing via KMS
SIGNER_SERVICE_URL=http://localhost:3003

# Internal API key for signer service (optional, for authentication)
# SIGNER_INTERNAL_API_KEY=
