Read GitHub issue #$ARGUMENTS using the github MCP.

Plan the implementation thoroughly. Cover:
- What needs to change (files to add/modify, key types/functions, tests).
- Open questions, ambiguities, or decisions that need my input.
- Risks and edge cases you can already see.

Post the plan as a single comment on the issue. Be explicit about
what you would *not* do, so I can correct scope before any code is
written.

Do not create a branch. Do not write code. Wait for my feedback in
the issue comments.
