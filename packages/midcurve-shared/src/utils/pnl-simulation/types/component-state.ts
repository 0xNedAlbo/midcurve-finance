/**
 * Base type for all component states.
 * Each component extends this with specific fields.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ComponentState {
  // Marker interface - components define their own state shape
}
